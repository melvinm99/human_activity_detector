class Configuration {
  static const String BASE_URL = 'https://loved-finally-trout.ngrok-free.app';
  static const Duration PROCESSING_TIME_INTERVAL = Duration(seconds: 20);
  static const Duration PREDICTION_REQUEST_TIME_INTERVAL = Duration(milliseconds: 500);
  static const int PREDICTION_SIZE = 3;
}